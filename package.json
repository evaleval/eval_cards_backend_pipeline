{
  "name": "eval-cards-backend-pipeline",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "pipeline": "python -m scripts.pipeline",
    "pipeline:dry-run": "python -m scripts.pipeline --dry-run"
  },
  "dependencies": {
    "@duckdb/node-api": "^1.5.1-r.1",
    "@huggingface/hub": "^2.11.0"
  }
}
